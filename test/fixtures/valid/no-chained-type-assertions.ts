const alpha = value as string
const beta = value as const as const
const gamma = (value as const) as const
const delta = <const>(<const>value)
const epsilon = (value satisfies string) as string
const zeta = (value as string)!
const eta = (alpha as string) + (beta as string)
const theta = format(value as string) as string
const iota = { first: value as string, second: value as number }
function kappa() {
  return (value as string).length
}
