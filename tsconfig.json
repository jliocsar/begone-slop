{
  "$schema": "http://json.schemastore.org/tsconfig",

  "extends": "../tsconfig/base.json",
  "include": ["src/**/*.ts", "test/**/*.ts"],
  // Deliberate violations: the rule tests' input.
  "exclude": ["test/fixtures/**"],

  "compilerOptions": {
    "types": ["bun"]
  }
}
